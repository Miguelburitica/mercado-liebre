const productModel = require('./product.model');
const userModel = require('./user.model');
const webModel = require('./web.model');

module.exports = {
	productModel: productModel,
	userModel: userModel,
	webModel: webModel,
};
